<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardClientRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Ligne supérieure : Nom et Suppression -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvClientName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#000000"
                android:text="NOM DU PATIENT"/>

            <ImageButton
                android:id="@+id/btnShareClient"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@android:drawable/ic_menu_share"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/brand_primary"
                android:layout_marginEnd="8dp"
                android:contentDescription="Partager"/>

            <ImageButton
                android:id="@+id/btnDeleteClient"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@android:drawable/ic_menu_delete"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/red"
                android:contentDescription="Supprimer"/>
        </LinearLayout>

        <!-- Adresse -->
        <TextView
            android:id="@+id/tvClientAddress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#444444"
            android:layout_marginTop="4dp"
            android:text="Adresse du domicile..."/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#EEEEEE"
            android:layout_marginVertical="12dp"/>

        <!-- Actions : Appel et Navigation -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Bloc Appel -->
            <LinearLayout
                android:id="@+id/llCall"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">

                <ImageView
                    android:id="@+id/ivCallIcon"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_call"
                    android:padding="6dp"
                    android:background="@drawable/item_client_background"
                    app:tint="#0D47A1"/>

                <TextView
                    android:id="@+id/tvClientTel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="#0D47A1"
                    android:text="06 00 00 00 00"/>
            </LinearLayout>

            <!-- Bouton Navigation -->
            <ImageButton
                android:id="@+id/btnNavClient"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:src="@drawable/ic_car"
                android:padding="12dp"
                android:scaleType="fitCenter"
                android:background="@drawable/item_client_background"
                app:tint="#1A237E"
                android:layout_marginStart="12dp"
                android:contentDescription="Navigation GPS"/>
        </LinearLayout>

        <!-- Notes -->
        <TextView
            android:id="@+id/tvClientNotes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#666666"
            android:padding="10dp"
            android:background="#F8F9FA"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            android:text="Notes de service..."/>

        <!-- Bouton de modification discret -->
        <Button
            android:id="@+id/btnEditClient"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MODIFIER LES INFOS"
            android:textSize="12sp"
            android:layout_gravity="end"
            android:layout_marginTop="8dp"
            style="@style/Widget.MaterialComponents.Button.TextButton"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>
